# Node
node_modules/
npm-debug.log
yarn-error.log

# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/

# Variáveis de ambiente locais
.env
.env.local

# Artifacts gerados pela pipeline
artifacts/

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# npm de versoes diferentes (local x VPS) reescreve o lockfile a cada install,
# e o diff sujo faz o agente parar no git pull. As dependencias sao 2 e estao
# cravadas no package.json — o lock nao paga o atrito.
package-lock.json
