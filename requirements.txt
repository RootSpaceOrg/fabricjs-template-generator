# GetPosts Pipeline v2 — Python dependencies
#
# A maioria dos scripts (review-html-design.py, audit-template-markup.py,
# suggestion-history.py, generate-placeholder-base64.py) usa apenas
# a stdlib do Python 3.8+.
#
# Apenas o uploader (gp2-template-uploader/scripts/import-template.py) exige
# dependência externa para falar com AWS STS + S3 + Lambda:
boto3>=1.26.0
