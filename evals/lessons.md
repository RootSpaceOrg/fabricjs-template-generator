# Lessons — rejeições e defeitos recorrentes

- 2026-08-03 (advance aceito com artefato fraco): runner validava existência, não validade — fixes avançou sem re-render e finalize entrou com 51 erros de marker. Correção no run.py: mtime do strip vs judge-report + fixes.md obrigatório; greps estruturais no template-marked.html (img sem data-image-type, editável sem descrição); validate-slides.js EXECUTADO pelo runner (exit 0 obrigatório); fidelity.md exige checklist [x] + "VEREDITO: FIEL". Também: advance aceito = começar próximo estágio imediatamente (regra de condução no PIPELINE.md).
- 2026-08-03 (all-blocked por contradição R1×placeholder): o placeholder canônico de professionalPhoto parece ilustração no screenshot, e a R1 era critério visual — todo candidato conforme era eliminado. Correção: R1 virou verificação de PROCEDÊNCIA (grep por professional-photo-1/2 no template.html, flag passada ao judge); aparência do placeholder é irrelevante porque o runtime troca pela foto do usuário. Pendente (opcional): substituir os b64 canônicos por cutouts fotográficos melhores — decisão do Gustavo.
- 2026-08-03 (execução travada no OpenClaw): sub-agentes morrendo sem artefato deixavam a pipeline em limbo — "disparei o sub-agente" era tratado como progresso; sem estado, retomadas recomeçavam do zero ou inventavam status. Correção: `bt/scripts/run.py` (runner com estado: advance só com artefato em disco, retomada via status, env imutável) + candidatos/judge com fallback sequencial na própria sessão quando delegação falha.
- 2026-08-03 (certificação de estilo bloqueada): `run.py` exige `fidelity.md` com `VEREDITO: FIEL` no estágio `finalize`, mas `FINALIZE.md` exige upload e `template_id` antes de abrir o editor e produzir essa fidelidade; `bt/scripts/upload.py` recusa executar fora do estágio `upload`. Resultado: não é possível concluir o corredor certificação via runner sem inventar evidência. Correção pendente: separar fidelidade pré-upload da fidelidade pós-editor, ou permitir upload de certificação no estágio finalize com um gate explícito.
- 2026-08-02 (fotobiomodulação run 2): **peça publicada ≠ peça aprovada** — fundos claros viraram dark (marker marcou neutros como data-variable), hero trocado após aprovação (imagem gerada pós-judge sem re-check), professionalPhoto perdido na conversão, decoração deslocada/de outra versão do HTML, dev virou prod, e o judge-report retro-editou a tabela R1 ("ok — corrigido depois"). Correções: regra zero de cadeia de custódia + neutros literais no marker + modo 3 do judge (fidelidade pós-swap) + gate de fidelidade visual pós-conversão + ambiente mecânico + relatório imutável. Gap restante (build): script SSIM determinístico pro gate de fidelidade.

- 2026-07-30 (fotobiomodulação 6 slides, 1º run seamless): fita monocromática dark nos 6 slides — faltava regra de ritmo tonal → adicionada a DESIGN.md (≥2 mudanças de fundo). Designer DESENHOU avatar flat em vez de usar o placeholder canônico de professionalPhoto (`references/placeholders/professional-photo-1/2.b64.txt`, cutout real) — violação de contrato de slot → regra dura no DESIGN.md + blocker eliminatório no JUDGE.md. Slides 4–5 com ~50% de área morta + pills estilo UI → regra de densidade + proibição de decoração-interface (DESIGN.md). Publicou "menos pior" sem barra mínima → barra de publicação criada (PIPELINE.md: total ≥30/50 E craft ≥6, 1 rodada de redesign, depois aborta).
- 2026-08-04 (deadlock fidelidade×upload, reportado pelo agente): upload.py recusava antes do estágio upload, mas o gate de fidelidade exige o template no editor. Correção: upload de teste permitido no estágio finalize (status review); estágio upload vira fechamento (set template_id + relatório).
- 2026-08-04 (certificação r3, conversão manual): agente emitiu TODOS os fills #141414 e fontSize 24 default (texto preto sobre preto na plataforma — pego pelo gate de fidelidade do Gustavo). Erros formulaicos também: _meta ausente, roundedRect sem cantos, manifest fora de output/. Correção: fix-fabric-fills.js (cores+tipografia dos computed styles) virou passo OBRIGATÓRIO pós-emissão no FINALIZE; patch de _meta/cantos documentado. Próximo build: fabric-blueprint do estilo elimina conversão manual de vez.
- 2026-08-05 (geração de imagem, veredito por evidência): Hermes via OAuth = 3 tentativas, 3 ilustrações disfarçadas de foto (doc oficial: codex é best-effort); OpenClaw (openclaw agent --agent main) = fotografia real de primeira. REGRA: imagem da fábrica gera-se via OpenClaw agent turn (ou API dedicada futura); tamanho de arquivo não é prova — verificação VISUAL obrigatória antes de usar.
- 2026-08-05: fita-v2 fidelidade plataforma==strip provada (var-teste-1/2). Fraqueza sistemica apontada pelo Gustavo: STORYTELLING — a camada copy (CONTEXT + knowledge/copy) precisa de feedback loops, o design tambem em detalhes.

- 2026-08-09 (deploy silencioso): a lei de transicao na emenda foi commitada, a VPS deu git pull e a certificacao inteira do clinical rodou SEM ela — o prompt do agente vem de /root/portal/comandos.py, uma COPIA, e o pull so atualiza o repositorio. Tres arquivos estavam defasados (app, comandos, knowledge). As fases 2 e 3 funcionaram na mesma rodada porque vivem no convert.js/pack.json/images.md, que o agente le do repo. Licao: mudanca em portal/ exige copia + restart, e o deploy.sh agora verifica com diff antes de dizer que terminou.
