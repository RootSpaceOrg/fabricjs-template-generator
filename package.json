{
  "name": "fabricjs-template-generator",
  "version": "0.1.0",
  "private": true,
  "description": "Fábrica de templates da mkt-platform — HTML to Fabric.js.",
  "scripts": {
    "postinstall": "node -e \"console.log('\\n[ok] playwright instalado. Agora rode: npx playwright install chromium (baixa o binario do navegador, ~150MB).\\n')\"",
    "check-playwright": "node -e \"const { chromium } = require('playwright'); chromium.launch().then(b => b.close()).then(() => console.log('playwright OK')).catch(e => { console.error('FAIL:', e.message); process.exit(1); })\""
  },
  "dependencies": {
    "fabric": "5.5.2",
    "playwright": "1.62.1"
  },
  "engines": {
    "node": ">=14"
  }
}
